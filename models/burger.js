var orm = require('../config/orm');

class Burger {
    constructor(id, name, devoured){
        this.id = id;
        this.name = name;
        this.devoured = devoured
    }

    update(){
    
    }
}

module.exports = Burger; 

